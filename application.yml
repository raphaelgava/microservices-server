#exemplo 4
spring:
  application:
    name: microservice

  logging:
    level:
      org:
        hibernate:
          SQL: DEBUG

#exemplo 4
#propertie específica da aplicação
greeting-service:
  greeting: "Hello"
  default-value: "My world"

#propertie para exposição das informações da aplicação pelo actuator
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'