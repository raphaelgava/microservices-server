# exemplo4
spring:
  application:
    name: greeting-service

  logging:
    level:
      org:
        hibernate:
          SQL: DEBUG

#propertie específica da aplicação
greeting-service:
  greeting: "Hola"
  default-value: "Mi mundo"

#propertie para exposição das informações da aplicação pelo actuator
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'

